{
  "private": true,
  "type": "module",
  "scripts": {
    "pretty": "prettier --write .",
    "typecheck": "tsc --noEmit",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "@types/node": "^24",
    "prettier": "^3",
    "typescript": "^5"
  }
}
